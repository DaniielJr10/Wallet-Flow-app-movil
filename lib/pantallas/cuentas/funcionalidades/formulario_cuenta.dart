// Widget para el formulario de agregar/editar cuenta
// Implementa aquí el formulario

import 'package:flutter/material.dart';

class FormularioCuenta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
