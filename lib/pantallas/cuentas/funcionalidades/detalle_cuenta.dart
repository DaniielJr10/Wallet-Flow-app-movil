// Widget para mostrar el detalle de una cuenta
// Implementa aquí el contenido de la vista de detalle

import 'package:flutter/material.dart';

class DetalleCuenta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
